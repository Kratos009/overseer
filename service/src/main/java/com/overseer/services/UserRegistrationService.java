package com.overseer.services;

import com.overseer.model.responses.Response;

public interface UserRegistrationService {
    // Response<Object> RegisterUser(UserCredentials userCredentials, UserProfile userProfile);
}
